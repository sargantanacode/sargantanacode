<% content_for :title, t('.edit_post') if @post.post? %>
<% content_for :title, t('.edit_page') if @post.static? %>

<div class="col-10 mb-3">
  <h2 class="text-center h2 mb-4">
    <%= t('.edit_post') if @post.post? %>
    <%= t('.edit_page') if @post.static? %>
  </h2>
  <%= form_for @post, :url => admin_post_path do |f| %>
    <div class="content mb-5">
      <%= render 'form_header', post: @post, new_post: false %>
      <%= render 'form_es', f: f %>
    </div>
    <div class="content mb-5">
      <%= render 'form_en', f: f %>
    </div>
    <div class="content">
      <%= render 'form_common', f: f, type: @post.type, new_post: false %>
    </div>
  <% end %>
</div>
