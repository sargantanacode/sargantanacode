<% content_for :title, t('.profile', name: @user.name) %>

<%= render 'posts/user-profile', user: @user %>

<% unless @user.posts.empty? %>
  <%= render 'posts/post-list', posts: @user.posts.status(:published)
      .type(:post).by_date.page(params[:page]) %>
<% end %>
