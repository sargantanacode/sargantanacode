<p><%= t('.welcome', name: @resource.name) %></p>

<p><%= t('.confirmation_message') %></p>

<p><%= link_to t('.confirmation_link'), confirmation_url(@resource, confirmation_token: @token, :locale => I18n.locale) %></p>

<p><%= t('.farewell') %></p>
