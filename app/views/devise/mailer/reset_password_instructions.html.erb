<p><%= t('.hello', name: @resource.name) %></p>

<p><%= t('.reset_message') %></p>

<p><%= link_to t('.reset_link'), edit_password_url(@resource, reset_password_token: @token, :locale => I18n.locale) %></p>

<p><%= t('.farewell') %></p>
