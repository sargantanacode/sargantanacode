<% content_for :title, @post.title %>

<%= render 'toolbar' %>

<%= render 'post' %>

<%= render 'social-buttons' %>

<% unless @post.static? %>
  <%= render 'user-profile', user: @post.user %>
<% end %>
